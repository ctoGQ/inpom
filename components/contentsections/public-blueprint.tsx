import { UniquePublicPage, type UniquePageConfig, publicImages } from "./unique-public-page"
export type Blueprint = { eyebrow: string; title: string; intro: string; image?: string; imageAlt?: string; themes: [string, string, string, string][]; actions: { label: string; href: string }[] }
export function PublicBlueprint({ data }: { data: Blueprint }) { const sections = data.themes.map(([eyebrow, title, text, extra], index) => ({ eyebrow, title, text, ...(index === 1 ? { image: publicImages.support, imageAlt: "Учасниці INPOM працюють разом" } : {}), ...(index === 3 ? { image: publicImages.trust, imageAlt: "Команда перевіряє результат проєкту" } : {}), bullets: extra.split(" • ") })) as UniquePageConfig["sections"]; return <UniquePublicPage config={{ ...data, sections, quote: "Кожна можливість стає сильнішою, коли її створюють разом." }} /> }
